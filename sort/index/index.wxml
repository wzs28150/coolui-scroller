<view class="coolui-scroller-nav">
  <slot></slot>
</view>
<block wx:if="{{overlay}}">
<view class="overlay" id="overlay" style="height:{{overlayHeight}}px;" animation="{{opacityAnimation}}" hidden="{{!isOverLayShow}}" ></view>
</block>